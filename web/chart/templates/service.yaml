apiVersion: v1
kind: Service
metadata:
  name: {{ include "tormenta-web.fullname" . }}
  labels:
    {{- include "tormenta-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "tormenta-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
